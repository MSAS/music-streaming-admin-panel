import { MimeToIconPipe } from './mime-to-icon.pipe';

describe('MimeToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new MimeToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
